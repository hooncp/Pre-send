import styled from 'styled-components'
import {Col} from 'react-bootstrap'

const NoPaddingCol = styled(Col)`
    padding: 1px !important;
    margin: 0 !important;
`
const KitCol = styled(Col)``

export {KitCol, NoPaddingCol};